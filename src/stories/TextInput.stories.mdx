import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { TextField } from "../components/TextInput";
import { withKnobs, boolean } from "@storybook/addon-knobs";
import { action } from '@storybook/addon-actions';

<Meta title="Design System|Text Input" component={TextField} />

# TextInput

TextInput are the building blocks for forms.

## Usage

TextInput are used to create simple - complex forms for web applications

## TextInput

TextInput.

<Preview withToolbar>
  <Story name="preFilled">
    <TextField placeholder={'User Name'} disabled={boolean("Disabled", false)}/>
  </Story>
</Preview>

